# === Supabase configuratie ===
# URL van jouw Supabase project
SUPABASE_URL=https://YOUR-PROJECT.supabase.co

# Service role key (⚠️ nooit publiek delen, alleen server-side gebruiken)
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# Optioneel: anon key (alleen nodig voor client-side testen)
SUPABASE_ANON_KEY=YOUR_ANON_KEY

# === Server configuratie ===
# Railway en Codespaces gebruiken deze variabele automatisch
PORT=8080

# === Scraper configuratie ===
# Timeout voor navigatie in milliseconden
NAV_TIMEOUT_MS=25000

# Wanneer Playwright moet wachten (bijv. "networkidle" of "load")
WAIT_UNTIL=networkidle

# === Feature flags ===
# Snapshot functionaliteit aan/uit
ENABLE_SNAPSHOT=true
