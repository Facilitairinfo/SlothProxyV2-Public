name: Update Feeds

on:
  schedule:
    - cron: "0 * * * *"   # elk uur op het hele uur
  workflow_dispatch:       # handmatig starten via GitHub UI

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Install Playwright browsers & deps
        run: npx playwright install --with-deps

      - name: Run scraper (incl. Supabase keep-alive)
        run: npm run scrape

      - name: Cleanup suffixed feed files
        run: npm run feeds:cleanup

      - name: Debug feed contents
        run: |
          echo "🔎 Aantal items per feed:"
          for f in feeds/*.xml; do
            count=$(grep -c "<item>" "$f" || true)
            echo " - $(basename "$f"): $count items"
          done

      - name: Pull latest main with rebase
        run: |
          git config --global pull.rebase true
          git pull origin main || true

      - name: Commit & push feeds
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add -f feeds/*.xml feeds/*.json
          git commit -m "Automatische feed update: $(date '+%Y-%m-%d %H:%M:%S')" || echo "⚠️ Geen wijzigingen om te committen"
          git push origin main
