{
  "name": "SlothProxyV2",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20",
  "remoteUser": "node",
  "forwardPorts": [8080],
  "postCreateCommand": "npm ci || npm install && npx playwright install || true && if [ ! -f .env ] && [ -f .env.example ]; then cp .env.example .env; fi",
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode"
      ]
    }
  }
}
